name: orchid_care_app_new
description: A new Flutter project
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2 # Flutter 3.3.0의 Dart 2.18.0과 호환되는 범위입니다.

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2
  table_calendar: ^3.0.8
  mobile_scanner: ^3.5.0
  camera: ^0.10.0+1
  stomp_dart_client: 0.4.4
  image_picker: ^1.1.0
  http: ^1.2.1

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true # 머티리얼 디자인 아이콘 사용을 명시합니다.